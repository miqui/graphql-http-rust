[package]
name = "graphql-http-rust"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
description = "HTTP-layer implementation of the GraphQL-over-HTTP specification (Stage 2 Draft): content negotiation, request parsing, response encoding, and status-code decision logic. Draft-stage software, not production-ready."
readme = "README.md"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]