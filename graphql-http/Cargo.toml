[package]
name = "graphql-http"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "HTTP-layer implementation of the GraphQL-over-HTTP specification (content negotiation, request parsing, response encoding, status-code decision logic)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[dev-dependencies]
